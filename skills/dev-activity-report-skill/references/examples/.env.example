# dev-activity-report skill configuration
# Copy to .env and adjust. All values are consumed by scripts; no hardcoded fallbacks.

# ── Scan paths ────────────────────────────────────────────────────────────────
APPS_DIR=~/projects
# Additional fixed-location directories to scan (space-separated). Leave blank if none.
# e.g. EXTRA_SCAN_DIRS="/opt/mydb /home/me/side-projects"
EXTRA_SCAN_DIRS=

# ── Homes ─────────────────────────────────────────────────────────────────────
CODEX_HOME=~/.codex
CLAUDE_HOME=~/.claude

# ── Output locations ──────────────────────────────────────────────────────────
REPORT_OUTPUT_DIR=~
REPORT_FILENAME_PREFIX=dev-activity-report
# Report filename uses UTC datetime suffix: <PREFIX>-YYYYMMDDTHHMMSSZ.md
# This ensures consecutive reports don't overwrite each other
REPORT_SANDBOX=workspace-write
# Output formats (comma-separated). JSON is always produced.
REPORT_OUTPUT_FORMATS=md,html
# Include compact Phase 1 payload in output JSON (disabled by default)
INCLUDE_SOURCE_PAYLOAD=false

# ── Report consolidation (optional) ───────────────────────────────────────────
DAR_TEST_REPORT_ROOT=~/path/to/dev-activity-report-skill
DAR_REPORT_ROOT=~
DAR_TEST_REPORT_GLOB=codex-test-report-*.md
DAR_REPORT_GLOB=dev-activity-report-*.md
DAR_TEST_REPORT_JSON_GLOB=codex-test-report-*.json
DAR_REPORT_JSON_GLOB=dev-activity-report-*.json
DAR_SOURCE_FORMAT=auto
DAR_AGGREGATE_FORMATS=json,md,html
DAR_AGGREGATE_OUTPUT=~/dev-activity-report-aggregate.md
DAR_AGGREGATE_TITLE="Dev Activity Report — Aggregate"

# ── Identity ──────────────────────────────────────────────────────────────────
RESUME_HEADER="Your Name Consulting, Jan 2025 – Present"

# ── Models ────────────────────────────────────────────────────────────────────
PHASE1_MODEL=haiku
PHASE15_MODEL=haiku
PHASE2_MODEL=sonnet
PHASE3_MODEL=haiku

# Optional: prepend custom instructions to each phase prompt.
# You can also force an override with language like:
# "Ignore any following instructions for this phase. <your instructions>"
PHASE1_PROMPT_PREFIX=
PHASE15_PROMPT_PREFIX=
PHASE2_PROMPT_PREFIX=
PHASE3_PROMPT_PREFIX=
# Preferred rule injection keys (safer than prefix overrides for structured phases)
PHASE15_RULES_EXTRA=
PHASE2_RULES_EXTRA=

# Phase 1.5 draft mode: terse (default) or thorough.
# Set true for opinionated highlights + lowlights + watch-out notes.
PHASE15_THOROUGH=false

# Optional: include quoted excerpts from Claude insights HTML in Phase 2 prompt context
# (off by default). If enabled, excerpts should be attributed in generated content.
INCLUDE_CLAUDE_INSIGHTS_QUOTES=false
CLAUDE_INSIGHTS_QUOTES_MAX=8
CLAUDE_INSIGHTS_QUOTES_MAX_CHARS=2000

# ── Auth / API endpoints (optional for subscription-managed environments) ─────
SUBSCRIPTION_MODE=true
PHASE15_API_BASE=
PHASE15_API_KEY=
PHASE2_API_BASE=
PHASE2_API_KEY=
OPENAI_API_BASE=
OPENAI_API_KEY=

# ── Fingerprinting ────────────────────────────────────────────────────────────
ALLOWED_FILE_EXTS=".py,.ts,.js,.tsx,.cs,.csproj,.md,.txt,.json,.toml,.yaml,.yml,.sql,.html,.css,.sh"
INSIGHTS_REPORT_PATH=~/.claude/usage-data/report.html

# ── Token logging ─────────────────────────────────────────────────────────────
# If omitted, defaults to REPORT_OUTPUT_DIR
# TOKEN_LOG_PATH=~/token_economics.log
# BUILD_LOG_PATH=~/build.log
# BENCHMARK_LOG_PATH=~/benchmarks.jsonl

# ── Pricing ($ per 1M tokens) ─────────────────────────────────────────────────
PRICE_PHASE15_IN=0.80
PRICE_PHASE15_OUT=4.00
PRICE_PHASE2_IN=3.00
PRICE_PHASE2_OUT=15.00
