# dev-activity-report skill configuration
# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

# ── Scan paths ────────────────────────────────────────────────────────────────
# Primary apps/projects directory (one per line if you want multiple — only
# the first value is used by the current skill; extend SKILL.md to loop if needed)
APPS_DIR=/lump/apps

# Additional fixed-location projects to include (space-separated, or leave empty)
# e.g. EXTRA_SCAN_DIRS="/usr/local/lib/mariadb /home/me/side-projects"
EXTRA_SCAN_DIRS=/usr/local/lib/mariadb

# ── Codex paths ───────────────────────────────────────────────────────────────
CODEX_HOME=~/.codex

# ── Claude paths ──────────────────────────────────────────────────────────────
CLAUDE_HOME=~/.claude

# ── Report output ─────────────────────────────────────────────────────────────
# Directory where the final report is written
REPORT_OUTPUT_DIR=~

# Report filename prefix (UTC datetime is appended automatically: <PREFIX>-YYYYMMDDTHHMMSSZ.md)
# This ensures consecutive reports don't overwrite each other
REPORT_FILENAME_PREFIX=dev-activity-report

# Output formats (comma-separated). JSON is always produced.
REPORT_OUTPUT_FORMATS=md,html
# Include compact Phase 1 payload in output JSON (disabled by default)
INCLUDE_SOURCE_PAYLOAD=false

# Optional log paths (defaults to REPORT_OUTPUT_DIR if omitted)
# TOKEN_LOG_PATH=~/token_economics.log
# BUILD_LOG_PATH=~/build.log
# BENCHMARK_LOG_PATH=~/benchmarks.jsonl

# ── Report header / identity ──────────────────────────────────────────────────
# Appears as the resume section header, e.g. "Acme Consulting, Jan 2025 – Present"
RESUME_HEADER=ngallodev Software, Jan 2025 – Present

# ── Model selection ───────────────────────────────────────────────────────────
# Haiku model used for Phase 1 data gathering (cheap, fast)
PHASE1_MODEL=haiku

# Codex model used for Phase 3 cache writes (deterministic, cheapest)
PHASE3_MODEL=gpt-5.1-codex-mini
